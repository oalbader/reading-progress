spring.application.name=reading-progress
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://65.21.63.64:5555/reading_progress}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:StrongPassword1233}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update

# Thymeleaf configuration
spring.thymeleaf.cache=false

# Server port
server.port=8080
